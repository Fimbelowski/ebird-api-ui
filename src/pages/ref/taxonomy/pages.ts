import type Page from '../../../types/Page';
import PAGE from './PAGE';

const pages: Page[] = [
  PAGE.EbirdTaxonomy,
  PAGE.TaxaLocaleCodes,
  PAGE.TaxonomicForms,
  PAGE.TaxonomicGroups,
  PAGE.TaxonomyVersions,
];

export default pages;
